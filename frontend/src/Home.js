import React from "react";
import Compiler from "./components/Compiler";
const Home = () => {
  return (
    <div className="container">
      <Compiler />
    </div>
  );
};

export default Home;
